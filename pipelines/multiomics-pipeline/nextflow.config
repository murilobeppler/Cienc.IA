params {
    outdir = 'results'
    reads = 'data/*_{1,2}.fastq.gz'
}

profiles {
    standard {
        process.executor = 'local'
    }
    docker {
        process.container = 'nextflow/nextflow:latest'
        docker.enabled = true
    }
}
